name: pages-disabled

# SuperLcm uses the GitHub repository README as its project home.
# The former standalone Pages deployment is intentionally disabled.
on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "SuperLcm documentation is published in the repository README; no standalone Pages site is deployed."
